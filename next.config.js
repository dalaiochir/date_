/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'] // хэрвээ гадаад зураг ашиглана гэвэл домэйн нэмнэ
  }
}

module.exports = nextConfig
